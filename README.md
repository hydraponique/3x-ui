# Руководство по установке форка 3x-ui с роутингом RoscomVPN

## Для чего?
- ✅ **Всегда актуальный роутинг RoscomVPN в каждой подписке (автообновляемый)**
- 👍 Все ваши конфиги, клиенты, подписки и настройки остаются без изменений
- 👍 Используется самая свежая версия 3x-ui на сегодняшний день

# Пошаговая инструкция

## ⚠️ Сначала выясним каким способом у Вас установлена панель 3x-ui:
#### Напишите команду в ssh на вашей VDS:
```bash
x-ui
```
➡️ Если команда ВЫВЕЛА результат, то вам подойдет способ - **"One Line Update"**

➡️ Если же команда НЕ НАЙДЕНА - то скорее всего панель установлена в контейнере Docker и Вам подойдет способ - **"Docker Update"**

-----

## Внимательно выберите способ на основе предыдущего шага:
### 1️⃣ **One Line Update** способ
#### 1.1. Обновление одной командой:
```bash
bash <(curl -Ls https://raw.githubusercontent.com/hydraponique/3x-ui/master/update.sh)
```
#### ✅ Готово! Форк успешно установлен и работает.

-----

### 2️⃣ **Docker Update** способ
#### 2.1. Переход в папку panel (местоположение 3x-ui)
```bash
cd panel
```
*Убедитесь, что вы находитесь в папке, где расположен файл `compose.yml`*

#### 2.2. Остановка текущего контейнера
```bash
docker compose down
```

#### 2.3. Редактирование файла конфигурации
```bash
nano compose.yml
```

#### 2.4. Замена образа в конфигурации
Найдите строку:
```yaml
image: ghcr.io/mhsanaei/3x-ui:latest
```

Замените на:
```yaml
image: ghcr.io/hydraponique/3x-ui:latest
```

**Внимание!**
- Сохраняйте пробелы и отступы
- Остальной конфиг оставляйте без изменений

*Управление в nano:*
- *`Ctrl+S` - сохранить изменения*
- *`Ctrl+X` - выйти из редактора*

#### 2.5. Обновление образа
```bash
docker compose pull
```

#### 2.6. Запуск контейнера
```bash
docker compose up -d
```

#### 2.7. Очистка старых образов
```bash
docker image prune
```
*Нажмите `Y` для подтверждения удаления*

#### ✅ Готово! Форк докер-контейнера успешно установлен и работает.
